# ESP-Iris system-inventory compile fixture

This internal fixture enables only the read-only system inventory provider.
It verifies that a normal application can expose post-reboot Flash evidence
without linking or advertising the recovery-only System Update writer.
